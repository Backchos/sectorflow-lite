[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "python -m streamlit run examples/app_streamlit.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
